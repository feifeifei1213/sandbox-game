# AGENTS.md

本文件定义 Codex CLI 在本仓库中的执行规范（作用域：仓库根目录及全部子目录）。

## 0) 核心执行原则（最高优先级）

1. **需求唯一来源**
   - 当前仓库的需求来源仅包括：用户当次明确指令、`docs/implementation_plan.md`、`game doc/Excel计算规则与跨表联动说明.md`、`game doc/*.xlsx`。
   - `game doc/山西航产科技有限公司沙盘模拟经营规则 .docx`、`game doc/game logic.md` 仅作为补充参考，不得直接视为本期唯一真实规则。

2. **严格按已确认共识推进**
   - 优先按 `docs/implementation_plan.md` 的模块与任务顺序推进。
   - 未经用户确认，不擅自扩大范围、引入重功能或改变首版边界。

3. **不擅自新增功能**
   - 只做当前任务清单中定义的内容。
   - 任何“顺手优化”“我觉得应该有”的功能，都必须先记录为建议，再等待用户确认。

4. **关键业务规则先确认再落地**
   - 出现游戏规则歧义、Excel 口径冲突、系统流程分叉时，先与用户确认，再实施。
   - 禁止在胜负规则、破产规则、年份联动、解锁规则等关键问题上自行拍板。

5. **先读规则文档，再做页面或代码**
   - 开始任何功能前，先阅读：
     - `docs/implementation_plan.md`
     - `game doc/Excel计算规则与跨表联动说明.md`
     - 相关 Excel / 原型文件
   - 先对齐规则和范围，再做 HTML Demo、需求文档或代码实现。

6. **进度必须实时回写**
   - 每完成一项任务，立即同步更新 `docs/implementation_plan.md`。
   - 回写内容至少包括：状态、落点、偏差说明、下一步。

7. **规则正确性优先于页面炫技**
   - 决策优先级：**规则正确 > 数据一致 > 流程稳定 > 页面体验 > 开发速度**。
   - 页面可以高保真贴 Excel，但底层规则不得“看起来像 Excel、实际上不一致”。

8. **Excel 公式链不得被静默改写**
   - 若系统设计与 Excel 公式链不一致，必须在文档中显式标出“差异点”和“确认结论”。
   - 禁止未经确认就把 Excel 的跨年联动、财报口径或汇总来源改成系统自定义逻辑。

9. **当前仓库默认产出是文档与原型**
   - 本仓库当前阶段主要产出包括：
     - 需求文档
     - 规则底稿
     - HTML 原型页
     - 后续代码设计说明
   - 在正式技术选型和目录结构确定前，不应擅自生成大量与需求无关的工程脚手架。

10. **语言规范统一**
   - 注释、设计说明、需求文档、任务记录使用中文。
   - 标识符命名（变量/函数/类/接口/表字段）使用英文，并遵循所选技术栈的既有风格。

11. **变更必须可追溯**
   - 任何规则调整、范围缩减、UI 方向变化，必须同步写入文档。
   - 保证“用户结论、计划文档、原型文件”三者一致。

## 1) 当前仓库状态（以当前文件为准）

- 当前仓库尚未进入正式编码期，主要处于：
  - 文档与规则持续收口
  - 首版编码与原型并行推进
- 当前主要文件：
  - `game doc/1组 最终版.xlsx`
  - `game doc/统计-汇总.xlsx`
  - `game doc/game logic.md`
  - `game doc/Excel计算规则与跨表联动说明.md`
  - `demo 网页/demo.html`
  - `demo 网页/excel-demo.html`
  - `demo 网页/excel-sidebar-demo.html`
  - `demo 网页/excel-combined-demo.html`
  - `player-demo.html`
  - `admin-demo.html`

## 2) 当前项目的稳定业务结论（已确认）

以下结论可作为当前首版的稳定共识：

- 有 `10` 个参赛组账号，`1` 个管理员账号。
- 每组在系统中按“1 个操作账号”建模。
- `0年` 为引导年/非排名年：
  - 玩家可填写、可提交
  - 参与到 `1年` 数据传递
  - 不进入正式汇总与最终排名
- `1年 ~ 最终年` 为正式比赛年。
- 季度由小组自行提交推进。
- 年度由管理员统一开放下一年。
- 经营页完成后，开放本年财报页。
- 财报页完成后，本年度完成。
- 玩家端经营页与财报页都采用 Excel 风格整页布局。
- 未开放阶段在页面中可见，但输入格保持锁定不可编辑。
- 经营页以“当前阶段必填校验 + 季末现金展示（供玩家线下自核）”作为首版核心防错机制。
- 财报提交时校验 `总资产 = 总负债 + 总负债权益`。
- 破产条件为：
  - 提交节点现金流断裂
  - 最终年度所有者权益为负
- 小组破产后停止后续填写，但历史数据保留。
- 首版管理员不做复杂阶段回退，改为“异常解锁”。

## 3) 规则来源分级

### 一级来源（主依据）

- 用户当次明确指令
- `docs/implementation_plan.md`
- `game doc/Excel计算规则与跨表联动说明.md`
- `game doc/1组 最终版.xlsx`
- `game doc/统计-汇总.xlsx`

### 二级来源（补充参考）

- `game doc/game logic.md`
- `game doc/山西航产科技有限公司沙盘模拟经营规则 .docx`

二级来源的使用边界：

- 可以帮助补充流程、术语和历史玩法背景
- 不能在与一级来源冲突时直接覆盖一级来源
- 不能把去年的规则默认当成今年规则

## 4) 当前文档与原型落点约定

### 文档

- 总实施计划：`docs/implementation_plan.md`
- Excel 规则底稿：`game doc/Excel计算规则与跨表联动说明.md`

### 原型

- 早期概念页：`demo.html`
- Excel 风格经营页：`excel-demo.html`
- 右侧窄栏探索版：`excel-sidebar-demo.html`
- 当前推荐混合版：`excel-combined-demo.html`

## 5) 当前阶段的产出要求

当前阶段默认按以下顺序推进：

1. 需求讨论与共识收敛
2. Excel 规则整理
3. 实施计划与 AGENTS 规范落地
4. HTML 原型验证
5. 技术选型与数据模型设计
6. 正式编码

在进入“正式编码”前，不应跳过文档与原型阶段。

## 6) 页面与系统设计约束

### 玩家端

- 首页默认进入 `0年经营`
- 年份标签全部可见，未开放年份不可进入
- 当前阶段可填区域为绿色输入格
- 未开放阶段输入格显示为锁定态
- 经营页和财报页都尽量贴近 Excel 结构
- 右侧仅保留轻量工作栏，不得破坏主表为主的布局

### 管理员端

- 菜单范围首版仅包括：
  - 汇总
  - 年度控制
  - 初始基线
  - 组数据
- 管理员端不做导出
- 管理员端不做复杂图表大屏
- 异常处理方式为“异常解锁”，不是复杂回退链路

## 7) 修改边界与禁止事项

- 不覆盖或破坏原始 Excel / Word 源文件。
- 不擅自修改已经确认的业务边界：
  - `0年` 非排名但参与传递
  - 季度玩家推进、年度管理员推进
  - 破产即淘汰
  - 首版不做导出
  - 首版不做复杂阶段回退
- 不把“去年的规则文档”直接当成“当前规则真相”。
- 不在未确定技术选型前生成大量后端/前端样板代码。

## 8) 文档更新要求

### 8.1 进度回写格式

每完成一个任务，至少回写：

- 任务状态：`✅ 已完成` / `🟡 部分完成` / `⏳ 未开始` / `⛔ 阻塞`
- 落点：具体文件路径
- 偏差说明：是否与原计划有出入
- 下一步：后续动作

### 8.2 跨文档联动

涉及规则变更、原型方向变化、首版范围调整时，至少检查并同步：

- `docs/implementation_plan.md`
- `game doc/Excel计算规则与跨表联动说明.md`
- 相关 HTML 原型文件

## 9) 冲突处理

- 与用户当次明确指令冲突时，以用户指令为准。
- 与更高优先级系统/开发者指令冲突时，以更高优先级指令为准。

## 10) 当前阶段的执行清单

### 10.1 开始任务前最少阅读

- `docs/implementation_plan.md`
- `game doc/Excel计算规则与跨表联动说明.md`
- 相关原型页

### 10.2 每次做需求决策时必须自查

- 这条规则是否已被用户明确确认？
- 这条规则是否与 Excel 当前链路冲突？
- 这条功能是否属于首版范围？
- 这条调整是否需要同步更新原型与文档？

### 10.3 进入编码前必须补齐

- 明确首版技术选型
- 明确核心状态机
- 明确主要数据模型
- 明确玩家端和管理员端的页面清单

## 11) 语言与质量规范

- 文档、注释、说明统一使用中文。
- 需求描述要具体，避免“支持管理功能”“支持查询能力”这类空话。
- 规则描述优先写“业务语义 + 触发条件 + 结果”。
- 不允许只写模糊口号，不写边界条件。
- 若存在不确定项，必须显式标记“待确认”，不能伪装成已确定规则。

