app:
  name: sandbox-game-service-group-count-test
  version: 0.1.0

server:
  host: 0.0.0.0
  port: 8080
  mode: debug
  readTimeoutSeconds: 15
  writeTimeoutSeconds: 15

frontend:
  distDir: frontend/dist

log:
  level: debug

mysql:
  dsn: root:123456@tcp(127.0.0.1:3306)/sandbox_game_group_count_test?charset=utf8mb4&parseTime=True&loc=Local
  maxOpenConns: 20
  maxIdleConns: 10

auth:
  mode: local
  tokenSecret: sandbox-game-group-count-test-secret
  tokenExpireSeconds: 28800
